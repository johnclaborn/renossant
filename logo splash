<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Renossant</title>
  <style>
    body {
      margin: 0;
      background: #ffffff;
      font-family: 'Barlow Condensed', sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      height: 100vh;
      padding-top: 60px;
      overflow: hidden;
      color: #aaa;
    }

    .logo {
      width: 87.5vmin;
      height: auto;
      opacity: 0;
      transform: scale(0.92);
      animation: fadeZoom 2s ease-out forwards, flicker 4s linear infinite;
    }

    .tagline {
      margin-top: 20px;
      font-size: 1.2rem;
      font-weight: 300;
      letter-spacing: 0.15em;
      text-align: center;
      opacity: 0;
      animation: fadeInTag 1.4s ease-out forwards;
      animation-delay: 2.8s;
      color: #999999;
    }

    @keyframes fadeZoom {
      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    @keyframes fadeInTag {
      0% {
        opacity: 0;
        transform: translateY(10px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes flicker {
      0%, 100% { opacity: 1; }
      3%, 5%, 7%, 50%, 90% { opacity: 0.96; }
      4%, 6%, 48%, 91% { opacity: 0.85; }
    }
  </style>
  <link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@300&display=swap" rel="stylesheet" />
</head>
<body>

  <img
    class="logo"
    src="https://johnclaborn.github.io/renossant/new%20logo%20edit%202025.png"
    alt="Renossant Logo"
/>

  <div class="tagline">Music and Film Production</div>

</body>
</html>
